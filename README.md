Para que esto funcione tienes que escribir en la terminal estando en el root folder del proyecto:

npm init -y

npm i --save-dev electron

npm install python-shell

npm install tabulator-tables --save